{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next"
        }
    ],
    "installCommand": "npm install --legacy-peer-deps && prisma generate",
    "bildCommand": "prisma generate && next build"
}